# Monitoring and Health Checks
- **Script**: `check-site.sh` monitors HTTP status and response times.
- **Automation**: Scheduled via Cron to run every 5 minutes.
- **Logging**: All checks are recorded in `/var/log/site_checks.log`.
